{
  "name": "ai-digital-twin-platform",
  "version": "1.0.0",
  "description": "AI Digital Twin SaaS Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
